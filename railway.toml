[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Image ENTRYPOINT runs migrate + `node server.js` (Next standalone).
# Do not override with `next start` — that binary is not in the slim image.
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
